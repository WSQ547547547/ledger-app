# 复制为 `.env`（与 package.json 同级）
# 在腾讯云开发控制台 → 环境 → 概览 / 安全配置 中获取

VITE_CLOUDBASE_ENV_ID=你的环境ID
VITE_CLOUDBASE_CLIENT_ID=你的Web安全应用ClientId
# 可选，默认 ap-shanghai（邮箱登录需上海地域）
VITE_CLOUDBASE_REGION=ap-shanghai
